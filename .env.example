# Paste your Feishu web login cookie here
# How to get: Login to feishu.cn web → F12 DevTools → Application → Cookies → Copy all as string
# Or use Playwright: context.cookies('https://www.feishu.cn') → join as "name=value; ..." string
LARK_COOKIE=
